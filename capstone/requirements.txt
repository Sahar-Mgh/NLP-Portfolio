# LuV-FaithCheck: reproduction dependencies.
# Versions pinned to the environment that produced the results in REPORT.md.
# Install:  pip install -r requirements.txt
#
# Runs on CPU; no GPU needed.
# If pip cannot find torch==2.10.0 for your platform, install a nearby torch
# from the official index first, e.g.:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
# then re-run this file; any torch >= 2.2 reproduces the results.

numpy==1.26.4
scikit-learn==1.3.0
sentence-transformers==5.2.3
transformers==4.57.1
torch==2.10.0

# Optional: only needed to rebuild the labeling worksheets, not to reproduce results.
# pandas==2.1.4
# openpyxl==3.1.5
