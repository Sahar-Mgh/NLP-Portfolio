# Normalise line endings so the repo is safe to clone on any OS.
* text=auto eol=lf

# Windows-only script keeps CRLF; POSIX shell script must stay LF (shebang).
*.ps1 text eol=crlf
*.sh  text eol=lf

# Treat spreadsheets as binary (no line-ending munging, no diff noise).
*.xlsx binary
